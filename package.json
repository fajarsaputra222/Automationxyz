{
  "name": "linknet-selenium",
  "version": "1.0.0",
  "description": "Selenium WebDriver tests for the Linknet application.",
  "main": "index.js",
  "scripts": {
    "generate-report": "mocha -r dotenv/config --no-timeouts --reporter allure-mocha",
    "view-report": "allure open allure-report --port 8080",
    "create-report": "allure generate allure-results --clean",
    "test": "mocha -r dotenv/config --no-timeouts"
  },
  "keywords": [
    "selenium",
    "webdriver",
    "testing",
    "mocha",
    "allure"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "allure-commandline": "^2.29.0",
    "allure-mocha": "^3.0.0-beta.3",
    "chai": "^5.1.1",
    "chokidar": "^3.6.0",
    "dotenv": "^16.4.5",
    "mocha": "^10.4.0",
    "selenium-webdriver": "^4.21.0"
  }
}
